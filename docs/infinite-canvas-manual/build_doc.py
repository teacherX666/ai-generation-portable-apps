#!/usr/bin/env python3
"""构建飞书文档《创意画布（无限画布）· 使用说明》。"""
from __future__ import annotations

import sys
from pathlib import Path

HERE = Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(HERE / "tools"))
from feishu_docx_builder import build_doc, load_client  # noqa: E402

DOC_TITLE = "创意画布（无限画布）· 使用说明"
ASSETS = Path(__file__).resolve().parent / "assets"

CONTENT = [
    ("quote", "面向 AI 美术同学：画布 = 一张无限大的工作台，素材、提示词、生成参数都用「节点」摆出来，连线即生成。"),
    ("divider",),
    ("h2", "一、这是什么？"),
    ("p", "创意画布是公司 AI 平台里的**节点式创作工具**。把参考图、提示词、生成参数摆成一个个节点、用线连起来，就能一次跑通「图生图 / 视频参考 / 批量生成」等组合流程——不用在多个模块之间来回搬素材。"),
    ("table",
     ["节点", "通俗解释", "怎么用"],
     [["提示词", "写生成要求，可引用参考图", "点开节点输入文字"],
      ["参考图片 / 视频 / 音频", "素材节点，放参考内容", "拖文件进画布自动建节点，或右键创建后上传"],
      ["图片生成 / 视频生成", "一张「生成订单」，含模型、尺寸、数量", "选模型，点「运行模型」"],
      ["文本", "纯文本（脚本 / 广告词等）", "点开节点输入文字"],
      ["组", "把多个节点打包成一个", "右键画布 → 组"]]),
    ("image", "A-canvas-home.png"),
    ("divider",),
    ("h2", "二、界面导览"),
    ("p", "在门户顶部点 **创意画布** 进入。左侧是画布库（所有项目），右侧是无限画布工作区："),
    ("bullet", ["**中间**：无限画布，右键呼出「新建节点」菜单，滚轮缩放、拖拽平移",
                "**左侧节点栏**：所有节点按类型分组，可折叠（「隐藏节点栏」按钮）",
                "**右下角**：「打开人像资产库」按钮与画布控制按钮"]),
    ("image", "B-canvas-empty.png"),
    ("divider",),
    ("h2", "三、快速上手：3 步出第一张图"),
    ("h3", "第 1 步：新建画布"),
    ("p", "点右上角 **新建画布**，自动进入一个空白画布。"),
    ("h3", "第 2 步：摆节点"),
    ("p", "**参考图直接拖进画布**，会自动建好「参考图片」节点；再在空白处**右键**依次创建「提示词」节点（写生成要求）和「图片生成」节点（选模型）。拖到顺手的位置。"),
    ("image", "C-canvas-menu.png"),
    ("h3", "第 3 步：连线、生成"),
    ("p", "从「参考图片」「提示词」节点的**右侧圆点**各拖一根线，连到「图片生成」节点左侧圆点；在生成节点里选好模型和尺寸，点卡片上的**运行模型**。结果会以「生成结果」新节点回到画布上，点结果可放大预览、下载。"),
    ("callout", "info", 2,
     "生成任务在服务端后台运行，关掉页面也照跑；回到画布就能看到结果节点。"),
    ("image", "D-canvas-nodes.png"),
    ("divider",),
    ("h2", "四、图片生成节点参数"),
    ("p", "「图片生成」节点就是生成订单的核心，点开能看到全部参数："),
    ("bullet", ["**模型**：下拉选择，按管理员授权显示；未授权会提示联系管理员",
                "**尺寸 / 分辨率**：预设档位（1K–4K）或自定义宽高（自动补成 16 的倍数）",
                "**数量**：一次出几张候选",
                "**运行模型 / 重试**：点「运行模型」提交；失败后可用「使用原任务键重试」原样重提"]),
    ("image", "E-canvas-config.png"),
    ("divider",),
    ("h2", "五、人像资产库"),
    ("p", "画布**右下角「打开人像资产库」**按钮 → 侧边面板。这里的素材与人像模块共用同一套方舟资产库："),
    ("bullet", ["**上传**：PNG/JPEG/WebP，10MB 内、宽高 300–6000px（方舟硬限制）",
                "**分组**：按组整理素材，下拉可切换组",
                "**选用**：点素材即可作为节点放到画布上",
                "**状态**：上传后显示「审核处理中」，就绪后为「已就绪」"]),
    ("image", "F-canvas-asset-library.png"),
    ("divider",),
    ("h2", "六、常用操作速查"),
    ("table",
     ["操作", "怎么做"],
     [["撤销 / 重做", "Ctrl+Z / Ctrl+Shift+Z（右键菜单也有）"],
      ["复制粘贴节点", "Ctrl+C / Ctrl+V"],
      ["缩放画布", "滚轮；右下角缩放按钮"],
      ["移动视角", "拖拽空白处"],
      ["删除节点", "选中后按 Delete，或右键「删除」"],
      ["取消生成中的任务", "结果/生成节点上的取消按钮"],
      ["结果派生", "对结果节点再连一个生成节点，把结果当素材继续生成"],
      ["归档项目", "项目列表勾选后「导出选中」成 zip"]]),
    ("divider",),
    ("h2", "七、常见问题（FAQ）"),
    ("p", "**Q1：右键菜单里没有某个节点类型？**\n节点栏（左侧）也能拖出全部节点；两者内容一致，按习惯用即可。"),
    ("p", "**Q2：提示「当前账号没有该模型的授权」？**\n模型按人授权，请管理员在「用户模型派发」里开通。"),
    ("p", "**Q3：上传素材提示失败？**\n检查格式（PNG/JPEG/WebP）、大小（10MB 内）与宽高（300–6000px）。"),
    ("p", "**Q4：生成节点一直转圈？**\n任务在服务端排队；结果回到画布后会自动停止。长时间无响应可点取消重提。"),
    ("p", "**Q5：怎么把画布成果发到群里？**\n在结果节点下载文件后发群即可；画布本身可导出 zip（项目列表的导出按钮）方便归档。"),
]


def main() -> int:
    client = load_client()
    doc_id = build_doc(client, DOC_TITLE, CONTENT, ASSETS)
    print("DOC_URL=https://redcqchina.feishu.cn/docx/" + doc_id)
    print("DOC_ID=" + doc_id)
    return 0


if __name__ == "__main__":
    sys.exit(main())
