# 修改计划 — 2026-06-25 维护窗口

> 范围：`/Users/260413a/ai-generation-portable-apps/`
> 备份：`backup/pre-fix-2026-06-25` 分支 + `v0.2.7-pre-fix-baseline` tag（仅本地，未 push）
> 回滚：`git checkout backup/pre-fix-2026-06-25 -- .`

---

## Context（为什么改）

1. **#15 Portal 假死**：同事跑 seedance/nano-banana 任务密集时，用户报"网站连不上"。我用 workflow 并行调研了 4 路（_proxy / VPN / 统计 / 前端），**修正了 pending-fixes 的根因判断**——`_proxy` 本身不会卡住 accept（ThreadingHTTPServer.daemon_threads=True），真因排序：
   - 浏览器同 origin 6 连接限制（轮询 + 静态资源 + 新 GET 排队）
   - `_proxy` 在 job 创建路径强制 buffer 整个 response body（`portal/app.py:1233-1256`），叠加 `self.rfile.read(length)`（1192）一次性读完上行 body
   - `ThreadingHTTPServer` 默认 `request_queue_size=5`，并发突增 SYN 被 drop
2. **VPN 抢路由**（pending-fixes #8）：当前 `get_lan_ip` 已隐式安全（ifconfig 私有段白名单），但 fallback `socket.connect(8.8.8.8)` 路径无 240/100.64/169.254 黑名单，且 `172.` 白名单覆盖到 172.32+ 公网段。
3. **孤儿端口循环**（pending-fixes #10）：`launchctl kickstart -k` 默认 SIGTERM 但实测过 SIGKILL → `finally manager.shutdown()` 跳过 → 子进程被 launchd 收养。`启动器.command` 与 launchd 是**两套并行启动路径**互不感知。
4. **运维流程缺失**（#14/#11）：手动重启踩坑没固化成脚本，没有部署文档。
5. **统计需求**：admin 想按"账户名 + 日期段"导出 Excel/JSON，UI 也要日期段选择器（不局限单日）。

---

## 不在本次范围内的（用户已澄清）

- 安全类：明文密钥、CSRF、X-Forwarded-User 隔离、uguu.se 图床、AK/SK 日志 print、dreamina install-cli — **用户明确不修**，公司局域网信任环境。
- 登录注册门槛（#9/#12/#13）— 已澄清"崩溃可以接受，不大改"。

---

## 任务概览

| # | 任务 | 优先级 | 文件 | 影响 | 重启 |
|---|---|---|---|---|---|
| 1 | #15 修 `_proxy` buffer 阻塞 | **P0** | portal/app.py | Portal + 4 子应用 | 是 |
| 2 | #8 VPN 抢路由（fallback 黑名单 + 172 边界） | P3 | portal/app.py | Portal 启动期 | 是 |
| 3 | #10 孤儿端口加固 | P3 | portal/app.py | Portal 启动期 | 是 |
| 4 | #14 safe_restart.sh | P3 | 新增脚本 | 0（运维） | 否 |
| 5 | #11 docs/deployment.md | P3 | 新增文档 | 0（文档） | 否 |
| 6 | 导出接口 `/api/platform/stats/export` | 新功能 | portal/app.py + 前端 | 仅新增 | 否 |
| 7 | 日期段查询 `/api/platform/stats/range` + UI | 新功能 | portal/app.py + 前端 | 仅新增 | 否 |

「重启」列指必须重启 Portal 才能生效；4/5 纯文档/脚本，6/7 是新代码可热加载（但首次需重启加载新 handler）。

---

## 详细方案

### 1. #15 修 `_proxy` buffer 阻塞（P0）

**根因总结**（已修正 pending-fixes 描述）：
- `_proxy` 在 job-creation 路径强制 `resp.read()` 读完整 body 才写 wfile（`portal/app.py:1235-1256`）
- 子应用 POST `/api/jobs` 入口（如 `seedance/app.py:1441`）已是异步 `threading.Thread(daemon=True).start()` + **立即返回** job_id，所以 _proxy 等 body 通常 ms 级
- 真正让用户"网站连不上"的是**浏览器 6 连接限制** + **listen backlog=5** + **每条慢请求多耗一个 Portal 线程**的叠加效应

**改造方案（B 方案：X-Job-Id 响应头）**：

1. **子应用层**（4 个 app.py）：所有 job 创建路径（`/api/jobs` POST 等）在响应里加 `X-Job-Id` header，body 保持原样不变：
   - `seedance/app.py` `do_POST` 写 job_id 时同步设 `self.send_header("X-Job-Id", job_id)`
   - `nano-banana/app.py` 同
   - `dreamina/app.py` 同
   - `volcengine-portrait/app.py` `/api/virtual/jobs` 与 `/api/real/jobs` 同

2. **Portal 层**（`portal/app.py:1172-1276` `_proxy`）：
   - 删除 `should_buffer = is_job or "application/json" in content_type` 中的 `is_job`，让 job 创建路径走 streaming 分支
   - streaming 分支在写 headers 前 `peek` `resp.getheader("X-Job-Id")`，调用 `tracker.register_job` 并继续 `shutil.copyfileobj` 流式回写
   - 保留原 `application/json` 走 buffer 的路径（响应小 + 前端要拿整 body）
   - **fallback**：如 `X-Job-Id` 缺失则记 warning，不影响下行

3. **listen backlog 增大**：在 `main()` 1346 创建 server 之后加 `server.socket.listen(64)`（默认 5，改 64 给突发并发余量）。

4. **请求体也不 buffer**（可选加分项）：把 `self.rfile.read(length)` 改成边读边 `conn.send(chunk)`。但这要求 `http.client.HTTPConnection` 支持流式 body — `conn.send(data)` 可以，但接口侵入大，**本次先不做**，仅做响应侧。

**改动行号清单**：
- `portal/app.py:1172-1276` `_proxy` 改 streaming 分支
- `portal/app.py:1346` 加 `server.socket.listen(64)`
- `seedance/app.py:1441` 附近的 do_POST job_id 返回处
- `nano-banana/app.py` 同模式
- `dreamina/app.py` 同模式（注意 `/api/text2image`/`/api/image2image`/`/api/text2video` 等多个 job 入口都要加）
- `volcengine-portrait/app.py` `/api/virtual/jobs` `/api/real/jobs`

**风险**：
- 子应用任一漏改 `X-Job-Id` → 统计丢失 + Portal warning 日志。修复：写一个测试 `curl -ki POST /seedance/api/jobs` 看 response header 有 `X-Job-Id`。
- 前端依赖 body 里 `job_id` 字段不变，不破坏。

### 2. #8 VPN 抢路由（P3）

`portal/app.py:61-87` `get_lan_ip`：

```python
PRIVATE_PREFIXES = ("192.168.", "10.")
# 172.16-31 是私有段；172.32-255 是公网，要逐一判断
def _is_172_private(ip):
    if not ip.startswith("172."):
        return False
    try:
        second = int(ip.split(".")[1])
        return 16 <= second <= 31
    except (ValueError, IndexError):
        return False

VPN_BLACKLIST_PREFIXES = ("240.", "100.64.", "100.65.", ..., "100.127.", "169.254.", "198.18.", "198.19.")
```

修改：
- ifconfig 解析路径：跳过 VPN_BLACKLIST，匹配 192.168/10/`_is_172_private`
- fallback `socket.connect(8.8.8.8)` 拿到 IP 后也走同样过滤；不通过则 fallback 到 `127.0.0.1` + 启动期 print 警告

**改动行号**：`portal/app.py:61-87` 完整重写 `get_lan_ip`，约 +20 行。

### 3. #10 孤儿端口加固（P3）

调研明确：
- `_kill_port_squatter`（350）已是"启动前无差别 kill 端口 listener"路径 — 这是**最佳兜底**
- 真正的孤儿来源是 `launchctl kickstart -k` 实测 SIGKILL，finally 跳过

加固：
- **`_kill_port_squatter` 加 retry**：调研报告评分最高的 B 方案已存在，但有 lsof timeout=5 单次。改成"如 kill 后再次 lsof 仍 LISTEN，retry 一次"
- **子进程组级 SIGKILL**：当前 `start_app` 用 `preexec_fn=os.setsid`（408），`shutdown` 用 `os.killpg(getpgid, SIGTERM)`（467）。但 SIGKILL 时这一步跳过。加 `atexit.register(manager.shutdown)`（线程已 daemon=True 不阻塞退出，atexit 是 Python 层 hook，不会救 SIGKILL，所以这一条作用有限）
- **真兜底**：portal 启动时（`main()`）在 `manager.start_all()` 之前先 `for port in [8787,8797,8888,8891]: _kill_port_squatter(port)`，无差别清残留（当前 start_app 已对每个 port 调用，但**写在主循环里**等于已经覆盖）。检查这一路径是否真生效——确认无误后无需改

**实际改动**：仅在 `_kill_port_squatter`（350-381）末尾加 retry（如 kill 后 sleep 0.5s 再 lsof，仍占则 sleep 1s 再 kill 一次），约 +10 行。

### 4. #14 safe_restart.sh（P3）

新增 `portal/state/scripts/safe_restart.sh`，做以下检查（用户取消则退出）：

```bash
#!/bin/bash
set -e
PORTAL=https://127.0.0.1:9090

# a. VPN 关闭确认（240.0.0.1 不存在）
ifconfig | grep -q "inet 240\." && { echo "VPN 还开着，先关 VPN"; exit 1; }

# b. 最近 5 分钟活动
curl -sk -m 3 $PORTAL/api/platform/activity \
  | python3 -c "import sys,json,time; d=json.load(sys.stdin); recent=[r for r in d.get('activity',[]) if time.time() - time.mktime(time.strptime(r.get('time',''), '%Y-%m-%d %H:%M:%S')) < 300]; print('recent:', len(recent)); exit(1 if recent else 0)"

# c. 各子应用 running jobs（避免杀掉跑批）
for app in seedance nano-banana dreamina volcengine-portrait; do
  curl -sk -m 3 "$PORTAL/$app/api/jobs" | python3 -c "..."
done

# d. 当前 lan_ip 与 cert 一致
diff <(cat portal/certs/lan_ip.txt) <(python3 -c "import sys; sys.path.insert(0,'portal'); from app import get_lan_ip; print(get_lan_ip())")

# e. 端口残留检查
for p in 8787 8797 8888 8891 9090; do lsof -ti:$p; done

# f. launchctl kickstart
launchctl kickstart -k gui/$(id -u)/com.ai-portal

# g. 重启后 5s 内 200 验证
sleep 5
curl -sk -m 5 $PORTAL/api/auth/first-run -o /dev/null -w "%{http_code}\n"
```

约 60 行 bash。

### 5. #11 docs/deployment.md（P3）

新增 `docs/deployment.md`，含 8 条要点（调研列出）：

1. **双路启动现状与正确入口**：launchd 守护是生产路径，`启动器.command` 仅本地开发用
2. **标准启停**：`launchctl bootstrap/bootout gui/$UID com.ai-portal`，重启用 `safe_restart.sh`
3. **plist 字段说明**：当前 `~/Library/LaunchAgents/com.ai-portal.plist` 内容截图 + 关键字段（Python 路径必须 `/usr/bin/python3` 系统 3.9，不要 homebrew 3.12）
4. **重启前必检**：VPN 已关、无 in-flight 任务
5. **cloudflared 已 unload**：要外网才 load
6. **LAN IP 漂移与 cert 重签**：`get_lan_ip` 不一致时自动重签 + 风险
7. **端口表**：8787/8797/8888/8891/9090 生产 + 8788/8798/8890/8892/9190 测试，**release zip 必须避开**
8. **孤儿排查 SOP**：`lsof -ti:PORT` / `ps -axo pid,ppid,command | awk '$2==1 && /app\.py/'` / `launchctl print`

约 150 行 Markdown。

### 6. 导出接口（新功能）

**接口**：`GET /api/platform/stats/export?start=YYYY-MM-DD&end=YYYY-MM-DD`

- **格式：CSV + UTF-8 BOM**（Excel/WPS 双击直显中文，纯 stdlib `csv` 模块写）
- **列布局：宽格式（一行一用户）**，列定义：
  ```
  username,
  seedance_images, seedance_seconds,
  nano-banana_images, nano-banana_seconds,
  dreamina_images, dreamina_seconds,
  volcengine-portrait_images, volcengine-portrait_seconds,
  total_images, total_seconds
  ```
  - `images` 列对图片任务计数（int）
  - `seconds` 列对视频任务的 done × duration 累加（int 秒）
  - `total_*` 是该用户在区间内跨所有 app 的汇总
- 数据源：`UsageTracker._data["by_user"]` 在 `[start, end]` 区间，对每用户 × 每 app 把 images / seconds 在区间内求和
- 权限：admin 看所有人；非 admin 只看自己（沿用 `get_history` 的 `role/username` 过滤）
- 默认参数：start 缺省 = 7 天前，end 缺省 = today
- 文件名：`usage-{start}-{end}.csv`
- HTTP 头：`Content-Type: text/csv; charset=utf-8` + `Content-Disposition: attachment; filename="..."`

**后端改动**：
- `UsageTracker` 新增 `export_range(start, end, username, role) -> list[dict]`
- `portal/app.py:840-846` 加 `/api/platform/stats/export` 路由 → `_platform_stats_export(user)`
- handler 写 CSV/JSON 时手动 send_header `Content-Disposition: attachment; filename="..."`

**前端改动**：
- `portal/static/app.js` 在 `StatsApp()` 加 `exportFrom / exportTo / exporting` 字段、`exportRange()` 方法（fetch+Blob，按 MEMORY 的"自签 HTTPS 下载修复"）
- `portal/static/index.html` 在「按日期查看」section（L554 附近）加日期段输入 + 导出按钮

### 7. 日期段查询（新功能）

**接口**：`GET /api/platform/stats/range?start=YYYY-MM-DD&end=YYYY-MM-DD`

- 返回 `{ ok, dates: [...], users: { name: { app: { images: [...N...], seconds: [...N...] } } } }` 与 `get_history` 兼容
- 实现：`UsageTracker.get_range(start, end, username, role)`，复用 get_history 的内层填充
- 默认：start 缺省 today-30，end 缺省 today
- 权限同 export

**后端**：
- `UsageTracker.get_range` 抽 helper（与 `get_history` 共享 dates 生成 + 内层填充）
- `portal/app.py` 加 `/api/platform/stats/range` 路由 → `_platform_stats_range(user)`

**前端**：
- 新增 `loadRange()` 方法，绑 range 选择器 onchange
- 在「按日期查看」section 把单日 input 升级为段（两个 input + 切换"区间/单日"）
- 渲染复用 history 的图/表逻辑

---

## 实施顺序

| Phase | 内容 | 测试方式 | 时长估计 |
|---|---|---|---|
| **A** | #11 deployment.md + #14 safe_restart.sh | 静态阅读，无需运行 | 30 min |
| **B** | #8 VPN 黑名单 + #10 retry 加固（小改动） | 测试端 9190 起，python3 -c "from app import get_lan_ip; print(get_lan_ip())" 验返回 | 20 min |
| **C** | 任务 6 导出接口（仅后端） | 测试端 curl /api/platform/stats/export 验 CSV BOM | 30 min |
| **D** | 任务 7 日期段查询（仅后端） | 测试端 curl /api/platform/stats/range 验数据形状 | 30 min |
| **E** | 任务 6+7 前端（StatsApp 改造） | 测试端浏览器打开 https://127.0.0.1:9190 看 UI | 40 min |
| **F** | #15 _proxy streaming + 4 个 sub-app X-Job-Id | 测试端 curl -ki POST /seedance/api/jobs 看响应头 | 60 min |

**A→B→C→D→E→F** 顺序执行，每完成一步在测试端验证再做下一步。最危险的 #15 放最后。

---

## 验证步骤（测试端 9190）

用户启动 `./Start\ Test.command` 后，我跑以下验证：

1. **#8 VPN**：`python3 -c "import sys; sys.path.insert(0,'portal'); from app import get_lan_ip; print(get_lan_ip())"` → 期望为私网 LAN IP（192.168.x.x / 10.x.x.x 等；IP 每周变化，具体值以当时为准；不能是 240.0.0.1 这类 VPN/保留段）
2. **#10 retry**：先手动 `lsof -ti:8788 | xargs kill -9` 杀掉测试 seedance，再 `curl -k https://127.0.0.1:9190/api/platform/status` → seedance 应 unhealthy 几秒后被拉起
3. **导出接口**：`curl -k -b "session=..." "https://127.0.0.1:9190/api/platform/stats/export?start=2026-06-20&end=2026-06-25" -o /tmp/test.csv && head /tmp/test.csv` → 期望首字节 BOM `\xef\xbb\xbf` + 第一行表头是 `username,seedance_images,seedance_seconds,nano-banana_images,...`
4. **日期段查询**：`curl -k -b "session=..." "https://127.0.0.1:9190/api/platform/stats/range?start=2026-06-20&end=2026-06-25" | jq .users` → 期望 8 个用户的 6 天数据
5. **#15 _proxy streaming**：
   - `curl -ki -X POST "https://127.0.0.1:9190/seedance/api/jobs/json" -d '{...minimal valid body...}'` → 响应头有 `X-Job-Id: ...`
   - 同时开 5 个 `curl` 并发跑 seedance job submit，验 Portal /api/auth/me 不卡（< 100ms 响应）
6. **#15 backlog**：`for i in {1..50}; do (curl -sk https://127.0.0.1:9190/ -o /dev/null &); done; wait` → 50 个并发请求都能在 5s 内完成（之前默认 backlog=5 会丢）

---

## 测试端使用方式

1. 用户双击 `./Start\ Test.command` → 端口 9190/8788/8798/8890/8892 启测试 Portal
2. 我通过 9190 跑 curl 验证 + 通过浏览器手动看 UI
3. 不影响生产 9090
4. 测试完用户双击 `./Stop\ Test.command` 关闭

---

## 提交策略

每个 Phase 完成一个独立 commit：

1. `docs: add deployment.md and safe_restart.sh`
2. `fix(portal): VPN 抢路由黑名单 + 孤儿端口 retry`
3. `feat(portal): stats export endpoint (CSV/JSON)`
4. `feat(portal): stats range endpoint`
5. `feat(portal): UI 日期段选择器 + 导出按钮`
6. `fix(portal/subapps): _proxy streaming + X-Job-Id header`

最后打 `v0.2.8` tag。**不 push origin**（用户已确认仅本地）。

---

## 回滚方式

任何一步出问题：

```bash
cd /Users/260413a/ai-generation-portable-apps
git checkout backup/pre-fix-2026-06-25 -- .
# 或具体某个文件
git checkout backup/pre-fix-2026-06-25 -- portal/app.py
```

测试端 / 生产端都用同一 git 工作树，回滚后重启 Portal 即恢复。
