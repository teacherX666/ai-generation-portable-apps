# 05 — 上游同步勘察（2026-08-28）

**前置**：`00`~`04` 册
**上游**：`~/ai-creation-canvas`，本次同步窗口 `6d1d2c6` → `8c355b3`（87 提交、178 文件、+13341/-1351）
**结论**：不能整体 re-vendor（本地 `infinite-canvas/web/src` 在 6d1d2c6 之上已有翻译补丁，PR #7 又动过 4 个文件），按功能挑选移植，与 2026-08-19 同步同模式。

---

## 1. 总体判断

上游这 87 个提交可归为 6 类。其中 3 类与我们的架构不兼容或不适用，直接跳过；3 类有真实增量可移植。

### 跳过（裁剪原则：Portal 自管身份/统计，providers.json 管模型配置）

| 上游功能 | 理由 |
|---|---|
| credential pool 增量合并 / 每 key 并发 4096 / 池计费费率 / usage 成本页（16d8fc7、c250e07、2c68079、bfdbb90、90b8a04、b73f016） | Portal 无池文件/计费概念 |
| admin 自助模型 onboarding（3aa4220） | 模型配置走 providers.json，700+ 行对不上现状 |
| pindo-gpt-image2 relay 渠道（6a435c2） | 渠道当配置加，无需代码 |
| mj 前缀账号管理（8c355b3 账号部分） | 属 Portal 域 |
| 皮肤系统（d19c6a7、9182b94、2606df2、主题链 1bac0bf~e6a235d） | 刚完成 PR #7 浅色统一；移植=重写 300-400 行映射，价值低，观感风险 |
| 任务中心列表/详情/下载（b24005a） | Portal 历史记录 tab 已是等价物（且架构上无统一 sqlite 可查） |
| 管理员看任意用户媒体（475490d） | 已覆盖（Portal 代理注入签名头，子应用 serve_file 不做 owner 检查） |
| 图片直接渲染卡片（b0464f1） | 已覆盖（history_update_terminal 写 thumb_url） |
| Seedance data-URI 参考视频（4aea56f） | 上游自己已否定（Ark 只收 https URL），目标走 TOS 收敛形态 |
| 参考视频关键词分类器（9b16244） | 目标 seedance 有显式任务模式下拉，更好 |
| ratio/duration 跟随参考视频提示（aab9a5b） | 目标文案已覆盖 |
| TOS 参考视频上传 / 肖像生视频（85bf654、b7b22c8） | 目标 seedance/app.py:1427-1455 + volcengine-portrait 已具备（预签名 vs public-read 均满足 Ark 未认证 GET） |
| 审核拒绝中文提示（fd5abff） | portal/ark_errors.py 已是加强版（10+ 条映射） |
| OpenAI 原生 t2i 适配器整体（7426a6e） | 目标 nano-banana 已等价；仅其 SSRF 防护下载模式可按需借鉴 |

---

## 2. 移植清单

### Tier 1 — 高价值低风险（12/12 已完成）

| # | 功能 | 上游提交 | 落点 | 改动量 |
|---|---|---|---|---|
| 1 | ✅ 画布节点自由缩放（角拖拽手柄，120×80~8192 钳制，resized 持久化，删除按钮式缩放工具栏）+ 结果图等比 contain | 47dc69a + a4f1716 | infinite-canvas/web/src（5+ 文件，无 server 契约） | 大 |
| 2 | ✅ 参考图缩略图点击全尺寸预览 + 结果节点双击预览（含 no-zoom 修复） | 59b06aa + cb1f3c9 | infinite-canvas/web/src（裁剪 var(--c-*) 主题 token，目标浅色硬编码） | 中 |
| 3 | ✅ 素材上传异步化（CreateAsset 后立即返回 processing，去 30s 同步轮询） | 66aecc3 | infinite-canvas/ark_library.py（目标轮询设施已就绪） | 小 |
| 4 | ✅ 素材面板加大 + 单素材删除（删方舟 + 删本地行） | 895068a | ark_library.py + app_fastapi.py + 面板 | 中 |
| 5 | ✅ 面板目标节点选择器 + 记住上次选择（itemCount 顺带） | ea58271 + 6307ed3 选择器部分 | 面板 + project.tsx | 小-中 |
| 6 | ✅ 拖文件进画布空白处 → 按 mediaType 在落点建素材节点 | 49cf8cd 拖拽部分 | project.tsx + InfiniteCanvas 已有 onDrop | 中 |
| 7 | ✅ Portal 服务端视频缩略图：`GET /api/platform/thumb?app=&url=` → ffmpeg 抽帧（-ss 0.2、480 宽、磁盘缓存 sha1、max-age 86400），HistoryApp 视频卡片 `<img>` 优先 + `<video>` 兜底 | 77c4802 + cfe65c3 机制 | portal/app.py ~80 行 + portal/static/app.js ~20 行；前置：宿主有 ffmpeg | 中 |
| 8 | ✅ 会话过期停止轮询（401 即停 + 提示，勿刷日志满 15 次退避） | 1470fa3 | portal/static/app.js dmPollOnce/pollJob | 小 |
| 9 | ✅ 参考视频时长校验（4-30 秒，实测拦截 + 中文报错 + 缓存） | 7c37c93 | seedance/static/app.js + portal/static/app.js seedance 表单 | 小 |
| 10 | ✅ portrait 素材组分页翻全量（cap 200，防旧组隐藏）+ 删后重列 | 849e699 分页部分 | volcengine-portrait/app.py handle_virtual_groups_get | 小 |
| 11 | ✅ 提示词技能内容并入（seedance-official 四段式 / seedance-director 导演方法论 / prompt-helper 分镜台词）| 9f674c0 + 11f88c3 | seedance/SKILL.md、director/SKILL.md 分节；director timeout 120→180s（c5d1c10） | 小 |
| 12 | ✅ nano-banana text2img 也可带参考图（有图即按 img2img 处理） | e5d45fd 语义 | nano-banana/app.py text2img 分支 | 小 |

### Tier 2 — 用户确认全带（3/4 已完成，分组体系实施中）

| # | 功能 | 上游提交 | 落点 | 改动量 | 备注 |
|---|---|---|---|---|---|
| 13 | 🔄 素材库分组体系（实施中）（方舟 CreateAssetGroup 族 + 组管理面板/资产页 + 导入上游独有素材 + 同源缩略图代理 + 失败原因上屏） | 96878f1、dc5cb6a、b59accb、7c25820、d13711d、9645ab1、5ad0d58、141a68c | ark_library.py +400 / app_fastapi.py 9 端点 / 前端面板 538 行 + 组浏览页 | 大 | 组 id 持久化沿用 JSON 文件惯例，不引 canvas_meta 表 |
| 14 | ✅ 画布参考视频端口（translate.py 声明端口 + 上传白名单 + ref_video_N 映射；seedance JSON 路径已支持 data_url） | fd7fcc0 + 0f1e36e 画布侧 | infinite-canvas/translate.py + app.py | 中 | 表单式 seedance 已有参考视频；画布侧是补平 |
| 15 | ✅ 失败解释模型兜底（本地规则 → doubao-seed 模型改写 ≤100 字 → 原文，三级降级；JOBS 内存 dict 去重替代 sqlite 列） | 792cae7 | seedance/app.py + volcengine-portrait/app.py；portal 历史弹窗自动受益 | 中 | 前置：Ark 账号需开通 doubao-seed 对话模型；未开通时优雅降级，可先实现 |
| 16 | ✅ 素材节点标题按类型顺延编号（参考图片1/2/3…） | 20d75c7 + 7ef3952 | web/src/features/graph + media-collection.ts | 小-中 | 纯锦上添花 |

### 不做（已裁决）

- 反推提示词（49cf8cd）：需方舟视觉模型直调通道（目标无此基础设施），另排期。
- 素材组 7 天自动清扫（2d25096 清扫部分）：目标只有一个默认组且无改选 UI，扫掉后 id 永久悬空 → 全部 CreateAsset 被拒。手动删组随 #13 做；自动清扫必须加「跳过当前默认组」保护。
- 皮肤系统 / 账号管理 / 计费 / model onboarding：见 §1 跳过表。

---

## 3. 关键适配提醒（移植时必读）

1. **主题适配**：上游是深色 + `var(--c-*)` token，目标是浅色硬编码（卡片 `#f8fafc`、边框 `#d9e0ea`、主色 `#235fd6`）。所有移植 diff 先剥 token 改动再重着色。59b06aa 提交说明自述夹带了未提交的主题 token 改动，必须剥离。
2. **自由缩放与工具栏互斥**：47dc69a 删除的按钮式工具栏正是 6ffd43b 修复的对象——做 #1 则 6ffd43b 自动作废。
3. **dd04ed7 四合一修复**：只取 (a) header-only 节点拖动（给媒体原生拖拽让路）+ (d) assets 页 Drawer→Modal；(b)(c) 依赖皮肤系统跳过。
4. **素材删除双删**：目标生成链读本地字节（translate.py），面板删除必须同时删方舟 + 本地行，否则素材占着列表。
5. **TOS 桶差异**：目标返回 12h 预签名 URL（私有桶），上游 public-read 桶 URL——均满足 Ark 未认证 GET，不动，代码注释互相印证即可。
6. **统计红线**：所有新端点不动 `_is_job_request` 白名单逻辑；若画布新加 job 提交路径，必须确认 X-Job-Id 上报。
