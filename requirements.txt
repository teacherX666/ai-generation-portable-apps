# Stage 2 dependencies. Install with:
#   python3.12 -m venv .venv
#   DYLD_LIBRARY_PATH=/opt/homebrew/opt/expat/lib .venv/bin/pip install -r requirements.txt
#
# DYLD_LIBRARY_PATH is required because Homebrew Python 3.12's pyexpat.so
# expects libexpat symbols not available in macOS's dyld shared cache;
# pointing it at Homebrew's own expat (2.8.1+) fixes the ImportError.
# Pinned to the versions verified in production (2026-07-22). Install into a
# .venv AT THE REPO ROOT — portal/app.py launches each sub-app's FastAPI engine
# via <repo>/.venv/bin/uvicorn, so the venv must live at the repo root.
# feishu-output-sync/ is pure stdlib and needs none of these.
fastapi==0.139.0
uvicorn[standard]==0.51.0
starlette==1.3.1
pydantic==2.13.4
httpx==0.28.1
python-multipart==0.0.32
pillow==12.3.0
pillow-heif==1.4.0
tenacity>=8.5

# rag-assistant（报错问答助手）依赖，版本锁定为 2026-08-26 生产实测可用版本。
langchain-core==1.6.0
langchain-openai==1.6.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
chromadb==1.5.9
lark-oapi==1.7.3
